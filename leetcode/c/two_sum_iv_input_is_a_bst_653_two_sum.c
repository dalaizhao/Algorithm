#include<stdio.h>

struct TreeNode{
    int val;
    struct TreeNode *left;
    struct TreeNode *right;
};

bool findTarget(struct TreeNode* root, int k){
    if(root == NULL){
        return false;
    }

}
